# onlyplans

A playful landing page showing Olaf's weekend schedule and plans.

## Hosting

This site is a static HTML page hosted with GitHub Pages from the `main` branch.

## Custom domain

After adding your domain in GitHub Pages, point your DNS to GitHub Pages:

- For an apex/root domain, add A records to:
  - `185.199.108.153`
  - `185.199.109.153`
  - `185.199.110.153`
  - `185.199.111.153`
- For `www`, add a CNAME record to `popcsev.github.io`.

Then tell me the domain and I can add the `CNAME` file and configure it in GitHub.
